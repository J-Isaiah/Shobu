services:
  backend:
    build: ./backend
    container_name: shobu-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: shobu-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend