services:
  backend:
    build: ./backend
    container_name: shobu-backend
    restart: unless-stopped
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: shobu-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend